// 主窗口配置
global.mainWinOptions = {
  width: 1038,
  height: 706,
  minWidth: 1038,
  minHeight: 706,
};

// 登录窗口配置
global.loginWinOptions = {
  width: 380,
  height: 550,
  maxWidth: 380,
  maxHeight: 550,
  minWidth: 380,
  minHeight: 550,
  frame: false, // 创建无边框窗口
  resizable: false, //禁止改变窗口尺寸
};
